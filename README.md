# Slides for the Object Orientation crash course

## Building
- Install `npm`.
- Install markdown-to-slides using `npm install markdown-to-slides`
- Run `markdown-to-slides slides.md -o slides.html`
- Open `slides.html` in a browser
